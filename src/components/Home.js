import React from "react";

import Feed from "../components/Feed/Feed";

const Home = () => {
  return (
    <section className="mainContainer">
      <Feed />
    </section>
  );
};
export default Home;
